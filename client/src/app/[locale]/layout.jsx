import "./globals.css";
import { ReduxProvider } from './providers';

export const metadata = {
  title: "Doran",
  description: "Generated by create next app",
};

// 'locale'을 params에서 받아와 lang 속성으로 사용
export default function RootLayout({ children, params }) {
  const { locale } = params; // params에서 locale을 추출

  return (
    <html lang={locale}> {/* locale을 lang 속성에 적용 */}
      <body>
        <ReduxProvider>
          {children}
        </ReduxProvider>
      </body>
    </html>
  );
}
