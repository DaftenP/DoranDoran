'use-client';

export default function StudyList({ level, className }) {
  return (
    <div
     className={`w-[10vh] h-[10vh] rounded-full z-1 ${className}`} 
     style={{ boxShadow: '0 3px 3px rgba(0, 0, 0, 0.3)'}} 
    >

    </div>
  );
}